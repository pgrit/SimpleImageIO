requires C++11 (or newer)

Build the C++ low level library (which has zero dependencies!)
```
mkdir build
cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
cmake --build . --config Release
cd ..
```

Build the C# wrapper
```

```

Run the tests
```
```

Package and publish
```
```
